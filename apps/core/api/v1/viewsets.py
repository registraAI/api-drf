from rest_framework import viewsets
# Nessa linha importar os serializers
# Nessa linha importar os modelos

# class NomeDoModeloViewSet(viewsets.ModelViewSet):
#     queryset = NomeDoModelo.objects.all()  # Substitua pelo nome do seu modelo
#     serializer_class = NomeDoModeloSerializer  # Substitua pelo nome do seu serializer
